#include <stdio.h>
#include <string.h>

int main() {
    char usuarioCorreto[] = "admin";
    char senhaCorreta[]   = "1234";

    char usuario[50];
    char senha[50];

    printf("=== SISTEMA DE LOGIN ===\n\n");

    printf("Usuario: ");
    scanf("%49s", usuario);

    printf("Senha: ");
    scanf("%49s", senha);

    if (strcmp(usuario, usuarioCorreto) == 0 && strcmp(senha, senhaCorreta) == 0) {
        printf("\nLogin realizado com sucesso!\n");
    } else {
        printf("\nUsuario ou senha incorretos!\n");
    }
/*  Versão modificada na branch sistema-de-login para teste de merge.*/
    return 0;
}
 
